apiVersion: app.terraform.io/v1alpha2
kind: AgentPool
metadata:
  name: agent-pool
  namespace: default
spec:
  organization: behnh
  token:
    secretKeyRef:
      name: tfc-operator
      key: token
  name: k8s-runners
  agentTokens:
    - name: token